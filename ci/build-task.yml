---
platform: linux

image_resource:
  type: docker-image
  source: {repository: maven}

inputs:
  - name: spboot

outputs:
  - name: spboot
#  - name: build-output

run:
  path: ./spboot/ci/task/build-task.sh